{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt && .venv/bin/mkdocs build --clean",
  "outputDirectory": "site",
  "installCommand": "echo 'skip npm install'",
  "framework": null,
  "trailingSlash": true
}
